# Copy to .env and fill in your real token.
# GitHub Personal Access Token (classic) — needs 'repo' + 'read:user' scopes.
# Create at: https://github.com/settings/tokens
GH_PAT=ghp_replace_me_with_real_token
